require "spec_helper"

RSpec.describe GoogleSpreadsheetBulkFetcher do
  it "has a version number" do
    expect(GoogleSpreadsheetBulkFetcher::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
